public struct Statistic {
    public let totalPokemonCount: Int
    public let caughtPokemonCount: Int
    
    public init(totalPokemonCount: Int, caughtPokemonCount: Int) {
        self.totalPokemonCount = totalPokemonCount
        self.caughtPokemonCount = caughtPokemonCount
    }
}
